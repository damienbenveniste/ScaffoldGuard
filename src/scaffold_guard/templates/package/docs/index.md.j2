# {{ project_slug }}

`{{ project_slug }}` is a strict Python package generated by `scaffold-guard`.

## Validate

```bash
scaffold-guard validate
```

## Explore

Start with `src/{{ package_name }}/core.py` and `tests/unit/test_core.py`.
