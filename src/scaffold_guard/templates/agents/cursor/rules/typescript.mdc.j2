---
description: "Strict TypeScript, Biome, and Vitest workflow rules."
globs: "src/**/*.ts, tests/**/*.ts, packages/typescript/**/*.ts"
alwaysApply: false
---

<!-- generated by scaffold-guard; edit scaffold-guard.toml or rerun scaffold-guard compile-rules -->

# TypeScript Rules

- Use `npm run` scripts for TypeScript commands.
- Keep `strict` TypeScript enabled.
- Do not add `any`, `// @ts-ignore`, broad casts, or broad suppressions.
- Keep source in `src/` for TypeScript-only projects and `packages/typescript/src/` for monorepos.
- Keep Biome, TypeScript, and Vitest validation green before claiming completion.
