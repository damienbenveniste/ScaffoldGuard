# {{ project_slug }}

Generated by `scaffold-guard`.

## Quickstart

```bash
uv sync --all-groups
npm install
scaffold-guard check
scaffold-guard inspect-diff
scaffold-guard validate
```

## Python Commands

```bash
uv run ruff format --check packages/python
uv run ruff check packages/python
uv run mypy packages/python/src packages/python/tests packages/python/examples
uv run pyright
uv run pytest packages/python/tests --cov={{ package_name }} --cov-report=term-missing --cov-fail-under={{ coverage }}
```

## TypeScript Commands

```bash
npm run ts:format:check
npm run ts:lint
npm run ts:typecheck
npm run ts:test
npm run ts:build
npm run ts:coverage
```

## Project Layout

- `packages/python/` contains Python package source, tests, and examples.
- `packages/typescript/` contains TypeScript package source and tests.
- `AGENTS.md` contains shared coding-agent instructions.
