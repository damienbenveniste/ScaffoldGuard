# {{ project_slug }}

Generated by `scaffold-guard` with the `minimal` profile.

## Quickstart

```bash
uv sync --all-groups
uv run scaffold-guard check
scaffold-guard validate
```

## Project Layout

- `AGENTS.md` contains shared coding-agent instructions.
- `pyproject.toml` pins ScaffoldGuard for repo-local `uv run` commands.
- `scaffold-guard.toml` contains ScaffoldGuard policy and adapter settings.
- Additional source, tests, docs, or app folders can be added when the project needs them.
