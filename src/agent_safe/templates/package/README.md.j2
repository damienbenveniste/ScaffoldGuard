# {{ project_slug }}

Generated by `agent-safe-python`.

## Quickstart

```bash
uv sync --all-groups
uv run agent-safe check
uv run agent-safe inspect-diff
uv run agent-safe validate
```

## Project Layout

- `src/{{ package_name }}/` contains package source.
- `tests/` contains unit and integration tests.
- `docs/` contains MkDocs documentation.
- `examples/` contains runnable examples.
- `AGENTS.md` contains shared coding-agent instructions.
