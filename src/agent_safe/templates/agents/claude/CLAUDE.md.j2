<!-- generated by agent-safe-python; edit agent-safe.toml or rerun agent-safe compile-rules -->

@AGENTS.md

## Claude Code Notes

- Treat `AGENTS.md` as the shared project instruction source.
- Use `.claude/rules/` for path-specific guidance when present.
- Instructions are guidance, not enforcement. Respect `agent-safe check` and CI as the source of truth for policy enforcement.
